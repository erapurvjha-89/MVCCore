@model IEnumerable<MVCWebEntityCore.Models.CourseCategory>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.CategegoryName)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.CategoryDescription)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CategegoryName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CategoryDescription)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CourseCategoryId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CourseCategoryId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CourseCategoryId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
